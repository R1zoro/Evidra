const { contextBridge, ipcRenderer } = require("electron");
contextBridge.exposeInMainWorld("evidraDesktop", { chooseDirectory: () => ipcRenderer.invoke("evidra:choose-directory"), chooseSource: () => ipcRenderer.invoke("evidra:choose-source"), tree: (root) => ipcRenderer.invoke("evidra:tree", root), readFile: (root, relative) => ipcRenderer.invoke("evidra:read-file", root, relative), writeFile: (root, relative, content) => ipcRenderer.invoke("evidra:write-file", root, relative, content), createFolder: (root, relative) => ipcRenderer.invoke("evidra:create-folder", root, relative), rename: (root, from, to) => ipcRenderer.invoke("evidra:rename", root, from, to), delete: (root, relative) => ipcRenderer.invoke("evidra:delete", root, relative), windowControl: (action) => ipcRenderer.send("evidra:window-control", action) });
